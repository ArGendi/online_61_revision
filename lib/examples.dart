// pick a date from calender
// DateTime now = DateTime.now();
// DateTime? selectedDate = await showDatePicker(context: context, firstDate: now, lastDate: DateTime(now.year, now.month + 1, now.day));
// if(selectedDate != null){
//   print(selectedDate);
// }

// pick a time (hour and minute)
// var selectedTime = await showTimePicker(context: context, initialTime: TimeOfDay.now());
// if(selectedTime != null){
//   print(selectedTime);
// }
